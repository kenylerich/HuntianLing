{
  "schemaVersion": 1,
  "role": "planner",
  "reqIds": [
    "REQ-AGENT-005"
  ],
  "outcome": "An executable workflow step inspects repository, CI, leases, and evidence before it runs, records the recognized state, and asks or waits instead of guessing the next transition",
  "acceptance": [
    "a caller reads a workflow run state snapshot including work item, leases, branches, and evidence",
    "recognizing a ready analyze step allows start and records the snapshot on the step",
    "an exclusive lease held by someone else blocks the step and posts a Team Chat event",
    "a missing approval names request-approval as the next safe action",
    "a stale snapshot after a design change blocks execution",
    "an agent run refuses to start from a blocked recognition",
    "customers cannot recognize workflow step state"
  ],
  "inScope": [
    "workflow run state snapshot",
    "step state recognition recorded on the step",
    "block execution when snapshot is stale, contradictory, or missing approval",
    "Team Chat event when recognition blocks or changes the planned action",
    "agent run refuses a blocked recognition"
  ],
  "outOfScope": [
    "visual workflow designer",
    "Workflow Test Lab simulation",
    "removing the fused board cockpit"
  ],
  "assumptions": [
    "workflow plan, start, leases, local Git, and delivery evidence already exist",
    "Test Lab simulation stays in a later D row"
  ],
  "openQuestions": []
}
