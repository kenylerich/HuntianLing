{
  "schemaVersion": 1,
  "role": "planner",
  "reqIds": [
    "REQ-FLOW-006",
    "REQ-FLOW-007"
  ],
  "outcome": "Authors edit and dry-run a template on a canvas before publishing",
  "acceptance": [
    "a caller reads a canvas of stages and steps for a template",
    "editing a draft canvas node updates the template step and reports invalid nodes",
    "builtin templates cannot be overwritten on the canvas",
    "validate reports a missing dependency before publish",
    "Test Lab dry-runs happy-path, missing-approval, resource-conflict, stale-state, and missing-evidence",
    "publish is blocked until a passing happy-path test run exists",
    "customers cannot edit the canvas or run the Test Lab"
  ],
  "inScope": [
    "versioned workflow canvas for a template",
    "draft canvas edits, validation, publish, and archive",
    "Workflow Test Lab dry-run of happy-path, missing approval, resource conflict, stale state, and missing evidence",
    "publication blocked without a passing happy-path test run"
  ],
  "outOfScope": [
    "third-party workflow pack conformance",
    "removing the fused board cockpit",
    "timeout, retry, custom-node, and pack conformance Test Lab scenarios"
  ],
  "assumptions": [
    "built-in templates, clone, select, and D1 state recognition already exist",
    "third-party packs stay in a later D row"
  ],
  "openQuestions": []
}
