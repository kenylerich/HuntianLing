/**
 * Workflow orchestration stub.
 *
 * TODO: encode the agile state machine declared in ./types.ts
 * (`WorkflowState`). The transitions will be driven by the harness
 * session events and the agile Service.
 */

export {};
