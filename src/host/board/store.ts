/**
 * Board persistence stub.
 *
 * TODO: choose the durable storage (local JSON for first cut; SQLite for
 * schema migration later — AGENTS.md requires monotonic `SCHEMA_VERSION`).
 * Until the choice is made, this module is intentionally empty so the
 * dependency direction (board/plugin.ts → board/store.ts) is observable.
 */

export {};
